# Example only. Do not commit real secrets.
# The Gradle build reads these values from local.properties or environment variables.
OPENFY_KEYSTORE_PATH=path/to/openfy-release.jks
OPENFY_KEYSTORE_PASSWORD=your_store_password
OPENFY_KEY_ALIAS=your_key_alias
OPENFY_KEY_PASSWORD=your_key_password

# GitHub Actions release workflow expects the keystore as a repository secret:
# OPENFY_KEYSTORE_BASE64=$(base64 -w 0 path/to/openfy-release.jks)
